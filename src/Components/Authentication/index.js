import React from 'react'
import styled from 'styled-components'

function Auth() {
  return (
    <SContainer>
      
    </SContainer>
  )
}

export default Auth

const SContainer = styled.div`
  display: flex;
`